local colors = require("themes.mi_tema").colors
return {
  normal = {
    a = { fg = colors.comment },
    b = { fg = colors.comment },
    c = { fg = colors.comment },
    x = { fg = colors.comment },
    y = { fg = colors.comment },
    z = { fg = colors.comment },
  }

}
