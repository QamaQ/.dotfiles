local options = {
  ensure_installed = { "lua", "vim", "pyrigth", "yaml", "bash" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
